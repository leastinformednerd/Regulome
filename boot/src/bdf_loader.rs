#![no_std]

extern crate alloc;

use lib::graphics::MonoFont;

fn load_bdf_to_mono_font(bdf_file: &str) -> MonoFont {
    let lines = bdf_file.lines();

    for outer_line in lines {
        
    }
}
